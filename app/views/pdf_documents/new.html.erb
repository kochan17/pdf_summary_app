<h1>Upload a PDF to Summarize</h1>

<%= form_with model: @pdf_document, url: pdf_documents_path, local: true, multipart: true do |form| %>
  <div>
    <%= form.label :file %>
    <%= form.file_field :file %>
  </div>
  <div>
    <%= form.submit "Upload" %>
  </div>
<% end %>
